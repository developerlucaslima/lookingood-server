version: '3.7'

services:
  api-solid-pg:
    image: bitnami/postgresql
    ports:
      - "5432:5432"
    environment:
      - POSTGRESQL_USERNAME=bnwserver
      - POSTGRESQL_PASSWORD=bnwserver
      - POSTGRESQL_DATABASE=bnwserver